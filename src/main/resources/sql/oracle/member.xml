<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skplanet.pandora.repository.oracle.OracleRepository">

	<select id="selectMerged" parameterType="map" resultType="com.skplanet.pandora.model.Member">
		SELECT MBR_ID
			 , OCBCOM_LGN_ID
			 , CI_NO
			 , MBR_KOR_NM
			 , CARD_NO
			 , SYW_MBR_ID
			 , EVS_MBR_ID
		FROM MBRT_MBR_ID_MST
		WHERE ${columnName} IN (SELECT COLUMN1 FROM TMP_${pageId}_${username})
	</select>

</mapper>
