<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skplanet.pandora.repository.oracle.OracleRepository">

    <select id="selectTest" parameterType="map" resultType="map">
        select #{a} as A from dual
    </select>

    <select id="selectTmp" parameterType="map" resultType="map">
        select * from TMP_${pageId}_${username}
    </select>


</mapper>
