<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skplanet.web.repository.mysql.UploadMetaRepository">

	<select id="selectUploadProgress" parameterType="map" resultType="UploadProgress">
		SELECT PAGE_ID
			, USERNAME
			, PARAM
			, FILENAME
			, STATUS
		FROM UPLOAD_PROGRESS
		WHERE PAGE_ID = #{pageId} AND USERNAME = #{username}
	</select>

	<update id="upsertUploadProgress" parameterType="map">
		INSERT
		INTO UPLOAD_PROGRESS ( PAGE_ID, USERNAME, PARAM, FILENAME, STATUS )
		VALUES ( #{pageId}, #{username}, #{param}, #{filename}, #{status} )
		ON DUPLICATE KEY
		<trim prefix="UPDATE" suffixOverrides=",">
			<if test="param != null and param != ''">PARAM = #{param},</if>
			<if test="filename != null and filename != ''">FILENAME = #{filename},</if>
			<if test="status != null">STATUS = #{status},</if>
		</trim>
	</update>

</mapper>
