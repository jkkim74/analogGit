<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skplanet.pandora.repository.mysql.MysqlRepository">

    <select id="selectUploadStatus" parameterType="map" resultType="com.skplanet.pandora.model.UploadStatus">
        SELECT UPLOAD_STATUS FROM UPLOAD_PROGRESS WHERE PAGE_ID = #{pageId} AND USERNAME = #{username}
    </select>

    <update id="upsertUploadStatus" parameterType="map">
        INSERT INTO UPLOAD_PROGRESS (PAGE_ID, USERNAME, UPLOAD_STATUS) VALUES (#{pageId}, #{username}, #{uploadStatus})
               ON DUPLICATE KEY UPDATE UPLOAD_STATUS = #{uploadStatus}
    </update>

</mapper>
